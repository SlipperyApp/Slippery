{
  "name": "slippery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "verify": "node build.mjs && node --test \"tests/**/*.test.mjs\" && node tools/audit.mjs && node tools/strings.mjs --check",
    "strings": "node tools/strings.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "@neondatabase/serverless": "^0.10.4"
  },
  "devDependencies": {
    "axe-core": "^4.10.2",
    "esbuild": "^0.28.2",
    "playwright-core": "^1.49.1"
  },
  "engines": {
    "node": ">=20"
  }
}

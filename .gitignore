node_modules
.next
.env
.env.local
.env*.local
.env.development
.env.production
*.tsbuildinfo
next-env.d.ts
test-results

# The video project installs its own toolchain, which is large and only
# needed to render. The rendered files in public/video/ ARE committed.
video/node_modules
video/out

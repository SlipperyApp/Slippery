# Slippery environment. Set these in Vercel → Settings → Environment Variables.
# Never commit real values: this repo is public and GitHub's secret scanner
# will auto-revoke anything that lands in a commit.

# ─── already set in Vercel ────────────────────────────────────────────────
ANTHROPIC_API_KEY=          # slip reading (/api/extract)
TELEGRAM_BOT_TOKEN=         # @SlipperyAppBot

# ─── needed for real accounts ─────────────────────────────────────────────
# Postgres. Vercel's Neon integration sets this for you in one click.
DATABASE_URL=
# Transactional email for verification codes. resend.com, free tier is plenty.
# WITHOUT IT SIGNUP STILL WORKS: accounts are created verified and signed in,
# because issuing a code nobody can receive would strand every signup. That
# means an address is unproven until you set this.
RESEND_API_KEY=
MAIL_FROM="Slippery <hello@yourdomain>"   # must be a domain verified in Resend

# ─── recommended, but the app runs without them ───────────────────────────
# Telegram echoes this on every update. WITHOUT IT THE BOT STILL RUNS, but
# the webhook URL will accept forged updates from anyone who guesses it.
#   openssl rand -hex 32
TELEGRAM_WEBHOOK_SECRET=

# ─── live settlement ──────────────────────────────────────────────────────
# Results are SCRAPED, no API needed. Three sources are tried in order —
# ESPN, then SofaScore, then football-data.org — the approach soccerdata
# takes: never depend on one site. ESPN leads because its per-period
# linescores prove the 90-minute score on an extra-time tie.
#
# All of them block by IP reputation, and every host gets a different
# answer. GET /api/sources from the deployment itself reports which ones it
# can actually reach. Nothing below is required for the app to boot.
#   football-data free tier: https://www.football-data.org/client/register
FOOTBALL_DATA_TOKEN=
# Pin one source by name: espn | sofascore | football-data | off.
# Unset tries them in order, which is what you want.
RESULTS_PROVIDER=
SCRAPE_UA=                  # optional override for the scrapers' User-Agent
SOFASCORE_UA=               # legacy alias, SofaScore only
# Protects /api/results from being called by anyone. WITHOUT IT THE SWEEP
# STILL RUNS: it falls back to trusting Vercel's own x-vercel-cron header,
# which is forgeable. Worst case is a wasted lookup, but set it anyway.
#   openssl rand -hex 32
CRON_SECRET=

# ─── optional ─────────────────────────────────────────────────────────────
PUBLIC_ORIGIN=https://slippery-iota.vercel.app
EXTRACT_MODEL=claude-haiku-4-5
